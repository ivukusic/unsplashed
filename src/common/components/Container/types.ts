import {
  BackgroundColorProps,
  BorderProps,
  FlexboxProps,
  LayoutProps,
  SpaceProps,
} from 'styled-system';

export interface Props
  extends BorderProps,
    SpaceProps,
    LayoutProps,
    FlexboxProps,
    BackgroundColorProps {}
